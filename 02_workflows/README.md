# 02_workflows — Operating Manual

Reusable task playbooks (skills) and quality gates (checklists).

- `skills/`: 18 task playbooks — trigger, steps, output, checklist enforcement
- `checklists/`: 7 quality gates — must pass before work is "done"
